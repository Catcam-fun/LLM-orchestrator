# Claude Rules — Anthropic (Claude Code)

This file is auto-loaded by **Claude Code** (Anthropic's coding CLI), which
hardcodes the `CLAUDE.md` filename. Sister stubs for other vendors:
- `AGENTS.md` — auto-loaded by **Codex CLI** (OpenAI) and the **Anthropic
  `agent` CLI** (both follow the AGENTS.md convention).
- `GEMINI.md` — auto-loaded by **Gemini CLI** (Google).

All three stubs intentionally point at the same source of truth — the
filename per tool is forced by the tool's hardcoded convention, but the
content is identical so every AI follows the same rules.

**ALWAYS** refer to `ai_main.md` as the source of truth. Follow execution
order in `ai_main.md`.
