"""scripts/ai_dougs/ — the dougs runtime + extracted helper modules.

Marked as a package so `from scripts.ai_dougs.ai_dougs import X` and
`from scripts.ai_dougs.cost_tracking import X` etc. work as standard
imports. Sibling `vault_graph/` was already a package; this brings
`ai_dougs/` to the same convention. Strictly additive (2026-05-09).
"""
